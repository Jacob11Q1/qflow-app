import { Users } from 'lucide-react'
import Placeholder from '@/components/Placeholder'

export default function Clients() {
  return (
    <Placeholder
      icon={Users}
      eyebrow="CRM"
      title="Clients"
      description="Every client, their projects, contacts, and the portal they log into — in one place."
    />
  )
}
